# markdown-editor
Terminal-based editor made in C supporting concurrent access by multiple clients

To run:

make all / make client, make server
./server <doc_update_time_interval>
./client <server_pid> <username>
